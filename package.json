{
  "name": "@f/hex-to-rgba",
  "description": "Convert a hexadecimal color code to an RGBA array",
  "repository": "git://github.com/micro-js/hex-to-rgba.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-number": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
